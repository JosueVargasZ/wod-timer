import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss';
import { WodTimerApp } from './WodTimerApp';

ReactDOM.render(
  <React.StrictMode>
    <WodTimerApp />
  </React.StrictMode>,
  document.getElementById('root')
);
